{
  "name": "workspace",
  "version": "1.0.0",
  "description": "This repository now contains the Capacitor + Vite project prepared for building RetroGunner on iOS. The application source lives inside `ios-retro-gunner/`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ios-sim": "^9.0.0",
    "py": "^0.0.0"
  }
}
